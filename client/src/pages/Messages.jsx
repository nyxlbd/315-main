import React, { useState } from 'react';
import ClientMessages from './ClientMessages';

function Messages() {
  // In the future, could add tabs for different message types
  return <ClientMessages />;
}

export default Messages;
